#include <Windows.h>
#include <stdio.h>

void main() {

	// 조건문 
	// 1) if  ==> 키워드
	// 2) (1) ==> 조건 ==> () 사이의 값이 1이면 실행 , 0이면 무시
	// 3) {}  ==> 실행 영역 
/*
	if (1) {
		printf("조건문1"); printf("\n");
	}

	if (0) {
		printf("조건문2"); printf("\n");
	}

	printf("점수를 입력하세요 ");
	int score = 0;
	scanf_s("%d", &score);
	if (60 <= score && score <= 100) {
		printf("합격"); printf("\n");
	}
	if (0 <= score && score < 60) {
		printf("불합격"); printf("\n");
	}

	// 변수는 { 안에서 선언되어서 } 끝날때 해제된다.
	int a = 10;
	{
		int b = 20;
	}
	printf("%d", a); printf("\n");
	printf("%d" , b);printf("\n"); 에러난다.
*/
}

